/*
 * Copyright 2025-2026 Yusuf Rençber
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

#[include "std/vec", "std/pair" : *]

class Map<K, V> {
    priv vector: Vector<Pair<K, V>>;

    Map(&mut this) {}

    Map(&mut this, other: &Map<K, V>) {
        this.vector = other.vector;
    }

    ~Map(&mut this) {}

    pub Data(&this) -> *const Pair<K, V> { ret this.vector.Data(); }
    pub Empty(&this) -> bool { ret this.vector.Empty(); }
    pub Size(&this) -> usize { ret this.vector.Size(); }

    pub Clear(&mut this) {
        this.vector.Clear();
    }

    pub Swap(&mut this, other: &mut Map<K, V>) {
        this.vector.Swap(other.vector);
    }

    pub Keys(&this) -> Vector<K> {
        let mut keys: Vector<K>;

        for pair in this.vector {
            keys.PushBack(pair.left);
        } ret keys;
    }

    pub Values(&this) -> Vector<V> {
        let mut values: Vector<V>;

        for pair in this.vector {
            values.PushBack(pair.right);
        } ret values;
    }

    pub Has(&this, key: K) -> bool {
        for pair in this.vector {
            if pair.left == key {
                ret true;
            }
        } ret false;
    }

    pub Get(&this, key: K, default: V) -> V {
        for pair in this.vector {
            if pair.left == key {
                ret pair.right;
            }
        } ret default;
    }

    pub Remove(&mut this, key: K) {
        for i in 0usize..this.vector.Size() {
            if this.vector[i].left == key {
                this.vector.RemoveAt(i);
                ret;
            }
        }
    }

    pub Index(&this, key: K) -> isize {
        for i in 0usize..this.vector.Size() {
            if this.vector[i].left == key {
                ret i as isize;
            }
        } ret -1isize;
    }

    pub op=(&mut this, other: &Map<K, V>) -> &mut Map<K, V> {
        this.vector = other.vector;
        ret this;
    }

    pub op[](&mut this, key: K) -> &mut V {
        for pair in this.vector {
            if pair.left == key {
                ret pair.right;
            }
        }

        this.vector.PushBack(Pair<K, V>(key));
        ret this.vector[this.vector.Size() - 1usize].right;
    }

    pub op[](&this, index: usize) -> &mut Pair<K, V> {
        ret this.vector[index];
    }

    pub Begin(&this) -> *mut Pair<K, V> {
        ret this.vector.Begin();
    }

    pub End(&this) -> *mut Pair<K, V> {
        ret this.vector.End();
    }
}