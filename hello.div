// include "divio" : *;

extern "C" {
    func printf(str: i8*, ...) -> i32;
}

func test(a: f64, b: f64) -> f64 {
    ret (31.0f64 + a * b) / 3.0f64 % (7.0f64 - 2.0f64);
}

func main() -> i32 {
    printf("Hello, World! = %f\n", test(1.0f64, 2.0f64));
    ret 0i32;
}