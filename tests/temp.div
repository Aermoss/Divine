include "divstr", "divvec", "divio" : *;

extern "C" {
    func StringFunc(input: i8*) -> String {
        let value: String;
        value = input;
        ret value;
    }

    func StringTest(input: i8*) -> String {
        let value: String = StringFunc(input);
        value += "String";
        ret value;
    }

    func VectorFunc(input: f64) -> Vector<f64> {
        let value: Vector<f64>;
        value.PushBack(input);
        ret value;
    }

    func VectorTest(input: f64) -> f64 {
        let value: Vector<f64> = VectorFunc(input);
        value.PushBack(10.0f64);
        ret value.PopBack() + value.PopBack();
    }
}