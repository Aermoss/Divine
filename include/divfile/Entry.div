/*
 * Copyright 2025-2026 Yusuf Rençber
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

include "divio", "divstd", "divwin", "divstr", "divpair" : *;

func ReadFile(path: *const i8) -> String {
    let mut content: String;
    let file: *const FILE = fopen(path, "r");
    let buffer[256u64]: i8;

    if file == null {
        printf("\x1b[31mFailed to open file: '%s'.\x1b[0m\n", path);
        ret content;
    }

    while fgets(buffer, sizeof(buffer) as i32, file) != null {
        content += buffer;
    }

    fclose(file);
    ret content;
}

func WriteFile(path: *const i8, content: *const i8) -> bool {
    let file: *const FILE = fopen(path, "w");

    if file == null {
        printf("\x1b[31mFailed to open file: '%s'.\x1b[0m\n", path);
        ret false;
    }

    fwrite(content, 1u64, strlen(content), file);
    fclose(file);
    ret true;
}

func GetTemporaryPath() -> String {
    let buffer[256u64]: i8;
    let length: DWORD = GetTempPathA(sizeof(buffer) as DWORD, buffer);

    if length > 0u32 && length < sizeof(buffer) as DWORD {
        ret String(buffer, 0u64, length as u64);
    } else {
        printf("\x1b[31mFailed to get temporary path.\x1b[0m\n");
        ret String();
    }
}

func JoinPath(dir: String, file_name: String) -> String {
    if dir.Empty() {
        ret file_name;
    }

    if dir[dir.Size() - 2u64] == '/' || dir[dir.Size() - 2u64] == '\\' {
        ret dir + file_name;
    } else {
        ret dir + '\\' + file_name;
    }
}

func PathExists(path: *const i8) -> bool {
    ret GetFileAttributesA(path) != INVALID_FILE_ATTRIBUTES;
}

func FileExists(path: *const i8) -> bool {
    let attributes: DWORD = GetFileAttributesA(path);

    if attributes != INVALID_FILE_ATTRIBUTES {
        ret (attributes & FILE_ATTRIBUTE_DIRECTORY) == 0u32;
    } else {
        ret false;
    }
}

func FolderExists(path: *const i8) -> bool {
    let attributes: DWORD = GetFileAttributesA(path);

    if attributes != INVALID_FILE_ATTRIBUTES {
        ret (attributes & FILE_ATTRIBUTE_DIRECTORY) != 0u32;
    } else {
        ret false;
    }
}

func RemoveFile(path: *const i8) -> bool {
    ret DeleteFileA(path) != 0i32;
}

func SplitExtension(path: String) -> Pair<String, String> {
    let mut i: u64 = path.Size();
    let mut index: u64 = path.Size();

    while i > 0u64 {
        i -= 1u64;

        if path[i] == '/' || path[i] == '\\' {
            break;
        }

        if path[i] == '.' {
            index = i;
            break;
        }
    }

    if index == path.Size() {
        ret Pair<String, String>(path, String());
    }

    let name: String = String(path, 0u64, index);
    let extension: String = String(path, index, path.Size());
    ret Pair<String, String>(name, extension);
}

func SplitPath(path: String) -> Pair<String, String> {
    let mut i: u64 = path.Size();
    let mut index: u64 = path.Size();

    while i > 0u64 {
        i -= 1u64;

        if path[i] == '/' || path[i] == '\\' {
            index = i;
            break;
        }
    }

    if index == path.Size() {
        ret Pair<String, String>(String(), path); 
    }

    let dir: String = String(path, 0u64, index);
    let file_name: String = String(path, index + 1u64, path.Size());
    ret Pair<String, String>(dir, file_name);
}